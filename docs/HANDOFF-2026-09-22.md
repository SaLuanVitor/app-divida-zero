# Handoff — 2026-09-22 (Orion → próxima sessão)

> Artefato de retomada. Estado verificado por leitura de git, sem executar portões.
> Projeto: **app-divida-zero** · Repo: `E:\Projetos\aiox\Projetos\app-divida-zero`

---

## 1. Estado do repositório

| Item | Valor |
|------|-------|
| Branch ativa | `feat/fase-5-seguranca` (sem upstream no origin — **100% local, nada pushado**) |
| HEAD | `d04eefe` |
| À frente de `origin/main` | 24 commits |
| Working tree | limpa (`git status` sem WIP) |
| Quem pode push | **somente @devops** (aiox-devops) |

`feat/fase-5-seguranca` não tem branch remota correspondente. Todos os commits abaixo
existem só localmente e só sobem quando @devops fizer o push.

### Commits da sessão (mais recentes primeiro)

```
d04eefe fix(mobile): align spacing/height/clamping with responsive + a11y standard
f72661a feat: gate email dispatch on SMTP + hide dev notif sender from non-admin
e6eb8ac feat(mobile): hide WhatsApp+email, add Open Finance hub, polish family screen
3724cb7 feat(mobile): telegram link identity + unlink + first-access prompt [Fase 3t]
121fe93 feat(backend): telegram unlink endpoint + me returns identity [Fase 3t]
e4de4c6 feat(mobile): add Telegram link screen per-user [Fase 3t]
4784c5b fix: keep DB data on restart (db:prepare) + scope telegram webhook to linking
4ebd803 feat(mobile): point API to fixed tunnel URL api.dividazeropa.sbs
8b4a766 feat(backend): telegram webhook endpoint + allow public hostnames
29cf470 docs: mark FASE-3t.1 TelegramChannel story done
5d6a78f feat(backend): add Telegram notification channel [Fase 3t.1]
6293330 docs: add TelegramChannel story (notification channel via Bot API) [Fase 3t]
b055aae docs: record Telegram pivot + SMTP out-of-scope + freeze bank sync on manual [ADR-0003]
f0529c3 docs: mark story 6.6 dark mode already implemented, defer WCAG audit
3edcd04 feat(mobile): confirm modal icon + haptic [Story 6.5]
0502c42 feat(mobile): relative notification timestamps [Story 6.4]
150047c feat(mobile): card variants + gradient; mark stories 6.2/6.3 [Story 6.3]
54e415f feat(mobile): add illustrated empty states across screens [Story 6.1]
33e7599 feat(mobile): add category donut and balance line charts [Story 5.7]
eb8a5dd docs: keep phone-keyed rate limiter per owner decision [Fase 3]
f54ee9b docs: mark Fase 3 abstraction + auth/opt-in tests done
fdc2ae4 feat(backend): add SMTP delivery test rake [Fase 1]
a96d130 feat(backend): fix channel DeliverResult namespacing + WA tests [Fase 3]
e4f2890 feat(mobile): add success animations with reduced motion [Story 5.6]
```

Outra branch relevante, já existente no origin:
`feature/10.4-migration-cleanup` — mantém os docs da Fase 4b0 (conector 200, freeze do auto-sync).

---

## 2. O que está concluído

- **Fase 5** (5.1–5.7): rate limiting, account lockout, session blacklist, audit log,
  haptic feedback, success animations (Lottie + reduced motion), gráficos de relatórios. ✅
- **Fase 6** (6.1–6.6): empty states ilustrados, card variants/gradiente, timestamps
  relativos, modal de confirmação com ícone + haptic, dark mode (já existia). ✅
- **Fase 3 (parcial)**: abstração de canais `ApplicationChannel` + `DeliverResult` e
  opt-in/opt-out prontos. WhatsApp abandonado, e-mail fora de escopo — ver ADR-0003.
- **Telegram (Fase 3t)** completo: `TelegramChannel`, bot `@appDividaZeroBot`, webhook,
  vínculo por usuário (deep link `/start <token>`), tela `TelegramSettings.tsx`,
  prompt de primeiro acesso no `Home.tsx`, unlink + identidade no `/auth/me`.
- **Open Finance hub** (`OpenFinance.tsx`): importar extrato, revisar transações,
  "sincronização automática — em breve" (congelada).

---

## 3. Decisões travadas (do dono — não reabrir sem ele)

Registradas em `docs/adr/ADR-0003-telegram-sem-whatsapp-sem-email.md`:

1. **Telegram substitui WhatsApp** como canal de notificação transacional.
   WhatsApp fica abandonado (código preservado, inerte, já gated).
2. **E-mail/SMTP fora de escopo** por ora — não há serviço de e-mail próprio.
   `EmailChannel` preservado, agora gated por `configured?` (`SMTP_PASSWORD`).
3. **Sync bancário congelado no manual OFX/CSV** (Fase 4a). Não existe agregador
   gratuito multi-usuário no Brasil (Conector 200 da Pluggy = 5 conexões pessoais).

---

## 4. Infra / ambiente local

| Item | Valor |
|------|-------|
| Domínio | `dividazeropa.sbs` (comprado na Hostinger, nameservers → Cloudflare `khloe`/`zahir`) |
| Túnel Cloudflare | named tunnel `divida-zero` (id `47dd8f64-ba3f-4a2f-8455-fff7bf5c9262`), config em `C:\Users\luanv\.cloudflared\config.yml` |
| URL pública fixa | `https://api.dividazeropa.sbs/api/v1` |
| Webhook Telegram | `https://api.dividazeropa.sbs/api/v1/telegram/webhook` |
| Bot | `@appDividaZeroBot`, token em `backend/api_divida_zero/.env` (gitignored) |
| chat_id do admin | `5914458140` |
| Docker Compose | `api-prod` (3000), `api-dev` (3001), `db` (Postgres 16) — **usa `db:prepare`** |
| Mobile env | `mobile/.env` e `mobile/.env.development` → `EXPO_PUBLIC_API_URL=https://api.dividazeropa.sbs/api/v1` |
| Tunnel script | `start-tunnel.ps1` (named tunnel via `--config`, não quick tunnel) |

Credenciais de admin de teste: `admin@test.com` (role admin), senha = `ADMIN_PASSWORD`
no `.env` da raiz. **O admin está atualmente DESVINCULADO do Telegram** (chat_id limpo
para testar o fluxo limpo).

---

## 5. Pendências / backlog

**Pequenas, opcionais (já mapeadas):**
- `Convidar.tsx`: ajustar o texto ("a pessoa precisa já ter conta").
- Revisão profunda de acessibilidade via `@ux-design-expert *a11y-check`.
- Código WhatsApp no backend: manter inerte (já gated), sem remoção estrutural.

**Ainda não iniciadas (próximas fases do roadmap):**
- **Fase 7** — Features avançadas (expo-local-authentication/biometria, netinfo/offline, Sentry).
- **Fase 8** — Preparação de lançamento.
- **Fase 4b1** — camada de provider bancário (adiada pela decisão de freeze).

**Validação com o dono:**
- Buildar/testar o mobile em device físico (`npx expo run:android` ou APK) — fluxo de
  vínculo Telegram + tela de notificações ainda não foram testados em aparelho.
- Push de `feat/fase-5-seguranca` (só @devops).

---

## 6. Armadilhas conhecidas (não repetir)

- **`docker-compose.yml`** usa `db:prepare`. NÃO voltar para `db:schema:load` (apagava o
  banco a cada restart e derrubava o vínculo do Telegram).
- **403 "Server: cloudflare"** = `config.hosts` do `production.rb` sem `api.dividazeropa.sbs`.
  A lista já contém os hosts corretos.
- **Webhook 404** = rota errada. A rota real é `/api/v1/telegram/webhook`, não `/telegram/webhook`.
- **Suíte backend completa em paralelo** dá segfault ("double free or corruption"). Rodar
  arquivo de teste por vez (< 50 testes = processo único).
- **`rails runner` inline no PowerShell** quebra com quoting (`&`, EOF). Usar script em
  `backend/api_divida_zero/tmp/`.
- **Túnel**: `cloudflared tunnel run` sem `--config` não carrega o ingress. Usar
  `cloudflared tunnel --config "$env:USERPROFILE\.cloudflared\config.yml" run divida-zero`.

---

## 7. Prompt de continuação (copiar e colar na próxima sessão)

```text
Retomar o projeto app-divida-zero (repo local em E:\Projetos\aiox\Projetos\app-divida-zero).
Leia primeiro docs/HANDOFF-2026-09-22.md para o estado consolidado.

Contexto rápido:
- Branch ativa: feat/fase-5-seguranca, HEAD d04eefe, 24 commits à frente de origin/main,
  NADA pushado (push é só do @devops). Working tree limpa.
- Fase 5 (5.1–5.7) e Fase 6 (6.1–6.6) concluídas. Fase 3 parcial: canais prontos, mas
  WhatsApp abandonado e e-mail fora de escopo (ADR-0003). Telegram é o canal ativo,
  com vínculo por usuário (deep link /start), webhook, tela de settings e prompt de
  primeiro acesso no Home.
- Decisões do dono (NÃO reabrir): Telegram no lugar do WhatsApp; sem SMTP/e-mail agora;
  sync bancário congelado no manual OFX/CSV.
- Infra local: API em Docker (api-prod na 3000), túnel Cloudflare named "divida-zero"
  para https://api.dividazeropa.sbs/api/v1, bot @appDividaZeroBot, chat_id do admin
  5914458140. Mobile aponta para esse domínio fixo (mobile/.env).

Armadilhas conhecidas: docker usa db:prepare (não db:schema:load); config.hosts já tem
api.dividazeropa.sbs; webhook em /api/v1/telegram/webhook; suíte backend roda 1 arquivo
por vez (segfault em paralelo); rails runner inline quebra no PowerShell (usar script em tmp/).

Me diga o que quer atacar nesta sessão, por exemplo:
(a) pequenas pendências opcionais (copy do Convidar.tsx, revisão a11y),
(b) iniciar Fase 7 (biometria/offline/Sentry),
(c) buildar/testar o mobile em device físico,
(d) preparar o push para o @devops.
```

---

## 8. Como este handoff foi produzido

Orion leu git (`branch`, `log`, `status`) e os docs do repo em 2026-09-22 e consolidou o
estado acima sem executar testes nem alterar código. Nenhuma mudança além deste arquivo
foi feita nesta etapa de handoff.
