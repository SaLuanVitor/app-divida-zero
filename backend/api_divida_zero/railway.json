{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "preDeployCommand": "bundle exec rails db:prepare && bundle exec rails app:bootstrap_admin; bundle exec rails db:seed",
    "startCommand": "bundle exec rails db:prepare && (bundle exec rails app:bootstrap_admin || true) && bundle exec rails db:seed && bundle exec puma -C config/puma.rb",
    "healthcheckPath": "/up",
    "sleepApplication": false,
    "useLegacyStacker": false,
    "ipv6EgressEnabled": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
